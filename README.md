# Image Preference: решение соревнования

Репозиторий содержит воспроизводимое решение задачи выбора лучшего изображения из пары сгенерированных картинок.

- **Целевая переменная:** `is_image1_better`
- **Метрика:** ROC AUC
- **Лучший public score:** **0.69417**
- **Результат:** решение подняло команду на **2-е место** public leaderboard на момент отправки.

## Кратко о подходе

Решение построено как двухэтапный пайплайн:
## Введение

Современные генеративные модели способны выдать по одному промпту десятки правдоподобных
изображений — но какое из них человек назовёт «лучшим»? Именно эту субъективную оценку
качества и пытается формализовать соревнование: научить модель предсказывать выбор экспертной
комиссии между двумя генерациями. Это близко к тому, как работают reward-модели
(PickScore, ImageReward, HPS), обучающие предпочтения человека для последующей настройки
генеративных пайплайнов.

Практическая сложность в том, что данных мало (8710 обучающих пар), промпт недоступен, а
«качество» часто прячется в мелких деталях — артефактах генерации, резкости, композиции.
Поэтому обучать тяжёлую сеть с нуля бессмысленно: выигрывает опора на сильные предобученные
представления и аккуратная работа с ранжированием пар. Ниже — как мы к этому пришли и что
получилось.

---

## Задача

1. **Frozen feature extraction.** Для каждого изображения извлекаются эмбеддинги из замороженных `timm` backbone. Признаки кэшируются в `.npy`, чтобы тяжелый GPU-этап не повторять при каждом эксперименте.
2. **Pairwise heads and rank blending.** На готовых эмбеддингах обучаются быстрые головы для сравнения пары: Bradley-Terry MLP и LightGBM. Финальное улучшение получено через rank-soup нескольких сабмитов.

Финальный ансамбль:

```text
75% DINOv2 submission
20% quality/artifact-head submission
 5% SigLIP submission
```
nn_2/
├── data/                      # сырые данные + предвычисленные .npy (локально)
│   ├── train.parquet
│   ├── test.parquet
│   ├── sample_submission.csv 
├── eda/                       # разведочный анализ (графики, eda.py)
├── image_preference/          # код пайплайна (зипуется на Kaggle)
│   ├── imgpref/               # пакет: config, features, models, pipeline, …
│   ├── configs/default.yaml   # гиперпараметры
│   └── notebooks/kaggle_run.py
├── ARCHITECTURE.md
└── README.md
```

Ранговое смешивание выбрано потому, что ROC AUC зависит от порядка объектов, а не от абсолютной калибровки вероятностей.

## Использованные backbone

| Backbone | Назначение |
|---|---|
| `convnextv2_base.fcmae_ft_in22k_in1k_384` | детали, текстуры, артефакты генерации |
| `vit_large_patch14_clip_224.openai` | CLIP-семантика и preference-сигнал |
| `vit_large_patch16_siglip_384.webli` | дополнительный vision-language сигнал |
| `vit_large_patch14_reg4_dinov2.lvd142m` | self-supervised структура и композиция |

## Структура ветки

```text
image_preference/        # пакет пайплайна: config, features, models, submit
kaggle/experiments/      # экспорт ключевых Kaggle-ячеек в .py
notebooks/               # оформленный Kaggle notebook
submissions/             # финальный lightweight submission CSV
docs/                    # журнал экспериментов и описание внешних артефактов
artifacts/               # маленькие отчеты; большие кэши не коммитятся
```

## Основные файлы

- `notebooks/ivan_german_image_preference_solution.ipynb` — аккуратно оформленный notebook с русскими markdown-пояснениями и сохраненными выводами ключевых ячеек.
- `submissions/submission_soup_dino75_quality20_siglip05.csv` — лучший сабмит этой ветки.
- `docs/EXPERIMENTS.md` — таблица экспериментов и public score.
- `docs/ARTIFACTS.md` — описание большого архива признаков `imgpref_dinov2_work.zip`, который не добавлен в git.

## Запуск на Kaggle

1. Подключить dataset соревнования `teta-nn-2-2026`.
2. Подключить код решения или этот репозиторий как Kaggle Dataset.
3. Для быстрого воспроизведения подключить архив признаков `imgpref_dinov2_work.zip` и распаковать его в `/kaggle/working/work`.
4. Открыть `notebooks/ivan_german_image_preference_solution.ipynb` или последовательно выполнить скрипты из `kaggle/experiments/`.
## Результаты

| Итерация | Что добавили | Public LB |
|---|---|---|
| Бейзлайн | один ConvNeXt-V2 @384 + BT + LightGBM | 0.6748 |
| Ансамбль | + DINOv2 + CLIP ViT-L, блендинг голов | 0.6876 |
| Тюнинг | метаданные в GBDT, multi-seed BT, OOF-блендинг | **0.6876+ (2-е место)** |

Стабильный паттерн: рост **OOF AUC** транслируется в рост публичного скора, поэтому OOF —
основной ориентир для решений. Сильнейший одиночный бэкбон — **CLIP** (логично: preference-модели
исторически строятся на CLIP).

Большой кэш `.npy/.npz` не хранится в git. Это сделано намеренно: репозиторий остается легким, а воспроизводимость обеспечивается через описание артефактов и финальный submission CSV.

## Заключение и идеи для улучшения

Мы прошли путь от бейзлайна на одном бэкбоне (0.675) до ансамбля, занявшего 2-е место (0.688),
за счёт разнообразия представлений и аккуратного блендинга по OOF. Что можно сделать дальше,
примерно в порядке ожидаемой отдачи:

1. **Более сильные / крупные бэкбоны.** CLIP тащит лучше всех → дать ему больше пикселей:
   `CLIP ViT-L @336`, а также **SigLIP @384** и **EVA-CLIP** — топовые экстракторы для
   «вкусовых» задач. Сигнал качества живёт в деталях, поэтому рост разрешения обычно даёт
   больше, чем ещё один похожий бэкбон.
2. **End-to-end файнтюн** (высокий потолок). Сиамский CLIP/ConvNeXt с Bradley–Terry лоссом
   вместо frozen-фич: LoRA или разморозка верхних блоков + layer-wise LR decay. Дороже по GPU,
   но потенциально самый большой прыжок.
3. **Специализированные preference-скоры как фичи.** Готовые **PickScore / ImageReward / HPSv2**
   и **LAION aesthetic predictor** — почти бесплатные ортогональные признаки (скор на картинку →
   разность в паре для GBDT).
4. **Богаче голова и CV.** CatBoost третьей моделью в блендинг; 10-fold вместо 5;
   больше сидов; калибровка/стэкинг вместо линейного rank-blend; `gbdt_pair_mode="full"`.
5. **Сильнее TTA.** Помимо hflip — multi-crop / multi-scale на этапе извлечения фич
   (усреднение эмбеддингов центра и углов).
6. **Псевдо-лейблинг.** Уверенные предсказания на test добавить в train — на маленьком датасете
   это иногда заметно помогает.
7. **Работа с асимметрией слотов.** `image_2` всегда WEBP; аккуратнее моделировать
   позиционный приор и артефакты кодеков (сейчас частично закрыто swap-аугментацией и мета-фичами).
